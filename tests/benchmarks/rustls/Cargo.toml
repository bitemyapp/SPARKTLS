[package]
name = "sparktls-rustls-bench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rustls = { path = "../../../third_party/rustls", version = "=0.23.45", default-features = false, features = ["std", "aws_lc_rs"] }
rustls-pemfile = "=2.2.0"

[profile.release]
debug = 1
